<VirtualHost *:80>
    ServerName ${HTTP_PRODUCTION_DOMAIN}
    DocumentRoot ${HTTP_PRODUCTION_ROOT}

    <Directory ${HTTP_PRODUCTION_ROOT}>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    SetEnv TYPO3_CONTEXT Production
</VirtualHost>
